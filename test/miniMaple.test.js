import {MiniMaple} from "../src/miniMaple";
import {spliter} from "../src/miniMaple";


test('0', () => {
    spliter();
});

/*
//!!!!!
//низя x*y
//зя x+y

test('1', () => {
    expect(diff("4*x^3", "x")).toBeEqual("12*x^2");
});

test('2', () => {
    expect(diff("4*x^3", "y")).toBeEqual("0");
});

test('3', () => {
    expect(diff("4*x^3-x^2", "x")).toBeEqual("12*x^2-2*x");
});
//============================================================================
test('4', () => {
    expect(diff("4*x", "x")).toBeEqual("4");
});

test('5', () => {
    expect(diff("4*x", "y")).toBeEqual("0");
});

test('6', () => {
    expect(diff("4*x^3-x^2-8*x^6", "x")).toBeEqual("12*x^2-2*x-48*x^5");
});

test('7', () => {
    expect(diff("4*x^3+x^2+8*x^6", "x")).toBeEqual("12*x^2+2*x+48*x^5");
});

test('8', () => {
    expect(diff("4*x^3-x^2+8*x^6", "x")).toBeEqual("12*x^2-2*x+48*x^5");
});

test('9', () => {
    expect(diff("4*x^3*y-x^2", "x")).toBeEqual("12*x^2*y-2*x");
});

test('10', () => {
    expect(diff("4*x^3*y-x^2", "x")).toBeEqual("12*x^2*y-2*x");
});

test('11', () => {
    expect(diff("4*x^3*y-x^2", "y")).toBeEqual("12*x^3");
});

test('12', () => {
    expect(diff("y", "y")).toBeEqual("1");
});

test('13', () => {
    expect(diff("45", "y")).toBeEqual("0");
});

test('14', () => {
    expect(diff("45+98", "y")).toBeEqual("0");
});

test('15', () => {
    expect(diff("45*x+98", "y")).toBeEqual("0");
});

test('16', () => {
    expect(diff("-4*x^3", "x")).toBeEqual("-12*x^2");
});

*/